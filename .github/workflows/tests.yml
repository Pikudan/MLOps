name: CI

on:
  push:
    branches:
      - '*'
  pull_request:
    branches:
      - '*'

jobs:
  tests:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r training/requirements.txt

      - name: Run tests
        run: |
          PYTHONPATH=${{ github.workspace }}/training:${{ github.workspace }}/training/src pytest training --maxfail=1 --disable-warnings -q

      - name: YOLO config dry run
        run: |
          python training/yolo/scripts/train_yolo.py training/yolo/configs/tomato_detect.yaml --dry-run --device cpu

